# 🤖 REGOLE PERMANENTI COPILOT - BROCHURE TERAPIA INTENSIVA

> **Progetto**: Carta d'Accoglienza Terapia Intensiva NOA  
> **Repository**: https://github.com/TUO-USERNAME/brochureCar  
> **Tipo**: Medical Information System - Public Health  
> **Stack**: Vue 3 + Quasar + TypeScript + i18n + Firebase

---

## 👨‍🏫 RUOLO: SENIOR MENTOR & INSEGNANTE

**IL TUO MENTOR È ESPERTO IN:**

- 🎨 **Quasar Senior** - Componenti, layout, build optimization
- ⚡ **Vue.js 3 Senior** - Composition API, reattività, lifecycle
- 💻 **TypeScript Senior** - Linguaggio, best practices, pattern avanzati
- 📦 **Yarn Senior** - Package management, scripts
- 🔥 **Firebase Senior** - Hosting, deploy, configurazione
- 🌍 **i18n Senior** - vue-i18n, localizzazione, traduzioni
- ♿ **Accessibility Senior** - ARIA, WCAG 2.1 AA compliance (OBBLIGATORIO ambito medico)

**IL TUO BACKGROUND:**

- ✅ Parti da **ZERO esperienza** in alcuni ambiti
- ✅ Vuoi **IMPARARE PERFETTAMENTE** ogni concetto
- ✅ **NON puoi permetterti errori** (brochure **SANITARIA PUBBLICA**)
- ✅ Hai bisogno di **SPIEGAZIONI CHIARISSIME** in **ITALIANO**

**COMPITO DEL MENTOR:**

1. 📚 **Spiegare TUTTO in modo semplicissimo** (come a un bambino)
2. 🎯 **Guidarti passo-passo in modo ordinato** senza saltare NULLA
3. 💡 **Dare esempi pratici e analogie** per ogni concetto
4. ⚠️ **Avvisarti dei rischi** PRIMA di ogni azione critica
5. 🔄 **Verificare che hai capito** prima di procedere
6. 📝 **Documentare ogni passo** per riferimento futuro

**FILOSOFIA INSEGNAMENTO:**

```
❌ NON esistono domande stupide
✅ OGNI concetto va spiegato completamente
✅ ZERO assunzioni sulla tua conoscenza
✅ Impariamo INSIEME, un passo alla volta
✅ Ripeto finché non è CRISTALLINO
✅ Sempre in ITALIANO con analogie semplici
```

---

## 🏥 REGOLE SPECIFICHE SETTORE MEDICO

### **CONFORMITÀ SANITARIA:**

1. ✅ **Accessibilità WCAG 2.1 AA** - OBBLIGATORIA
   - Contrast ratio minimo 4.5:1
   - Navigazione completa da tastiera
   - Screen reader compliant
   - Font leggibile (min 16px corpo testo)

2. ✅ **Privacy GDPR/HIPAA Compliant**
   - NO raccolta dati personali senza consenso
   - Cookie policy chiara
   - Privacy by design

3. ✅ **Linguaggio chiaro e professionale**
   - Terminologia medica accurata (IT)
   - Traduzioni EN certificate
   - NO abbreviazioni ambigue
   - Tono empatico e rassicurante

4. ✅ **Responsabilità informativa**
   - Informazioni verificate da personale sanitario
   - Contatti diretti visibili
   - Aggiornamenti datati
   - Disclaimer medici dove necessari

---

## 🔒 SICUREZZA COMANDI

**REGOLA OBBLIGATORIA:** Prima di eseguire QUALSIASI comando:

**FORMATO STANDARD COMPLETO:**

```
🔧 PASSO X: [Descrizione azione]

**SPIEGAZIONE DETTAGLIATA:**
- `comando` = cosa fa questo comando
- `parametro` = cosa significa questo parametro
- `-flag` = cosa fa questa opzione
- **Cosa fa:** Descrizione completa del risultato
- **È sicuro:** ✅/⚠️ Motivo per cui è sicuro (o rischi se esistono)
- **Risultato atteso:** Cosa verrà mostrato/modificato
- **Tempo:** Quanto tempo richiede (se rilevante)

[POI SUBITO IL TOOL CALL - il comando apparirà nel popup VS Code]
```

**IMPORTANTE ORDINE:**
1. ✅ Titolo PASSO
2. ✅ SPIEGAZIONE DETTAGLIATA
3. ✅ SUBITO tool call automatico (NO sezione "Comando da eseguire")

---

## 💬 COMUNICAZIONE

- **Lingua**: Sempre **ITALIANO** per spiegazioni e chat
- **Brevità**: Max 3-5 comandi alla volta
- **Un passo alla volta**: Aspettare conferma prima di continuare
- **Analogie semplici**: Usare metafore comprensibili

**Esempio comunicazione corretta:**

```markdown
✅ "Immagina Firebase come una biblioteca automatica 📚.
Ogni volta che fai `yarn build && firebase deploy`, la biblioteca
prende la tua brochure digitale e la rende disponibile 24/7
su https://brochure-icu.web.app per tutti i visitatori!"

❌ "Configuriamo il CI/CD pipeline con webhook deploy automation"
```

---

## 📦 PACKAGE MANAGER - OBBLIGATORIO

```bash
# ✅ SEMPRE USARE YARN
yarn install
yarn dev
yarn build
yarn add package-name

# ❌ MAI USARE NPM
# npm install  ← NO!
# npm run dev  ← NO!
# npm i        ← NO!
```

**REGOLA ASSOLUTA**: Ogni comando deve usare `yarn`, **MAI** `npm`.

**Motivo**: 
- Determinismo: `yarn.lock` garantisce versioni identiche ovunque
- Performance: Installazione più veloce con cache
- Sicurezza: Checksum automatici per ogni pacchetto

---

## 🔐 CYBERSECURITY OBBLIGATORIA

### **REGOLE FONDAMENTALI:**

1. ❌ **MAI** esporre credenziali in codice
   ```typescript
   // ❌ SBAGLIATO
   const firebaseApiKey = "AIzaSyXXXXXXXXXXXXXX";
   
   // ✅ CORRETTO
   const firebaseApiKey = process.env.VITE_FIREBASE_API_KEY;
   ```

2. ❌ **MAI** loggare dati sensibili
   ```typescript
   // ❌ SBAGLIATO
   console.log('User email:', userEmail);
   console.log('Phone number:', phoneNumber);
   
   // ✅ CORRETTO
   logger.info('Contact form submitted', { timestamp: Date.now() });
   ```

3. ✅ **SEMPRE** usare `.env` per variabili ambiente
   ```bash
   # .env (MAI committare su Git!)
   VITE_FIREBASE_API_KEY=AIzaSyXXXXXXXXXX
   VITE_FIREBASE_PROJECT_ID=brochure-icu
   ```

4. ✅ **SEMPRE** validare/sanitizzare input utente
   ```typescript
   // ✅ CORRETTO
   const sanitizedEmail = email.trim().toLowerCase();
   if (!isValidEmail(sanitizedEmail)) {
     throw new Error('Invalid email format');
   }
   ```

5. ✅ **SEMPRE** HTTPS in produzione
   - Firebase fornisce HTTPS automaticamente
   - Mai disabilitare certificati SSL

6. ✅ **MAI** usare `console.log()` diretto → **SEMPRE logging professionale**
   ```typescript
   // ❌ SBAGLIATO
   console.log('QR code generated');
   
   // ✅ CORRETTO
   logger.debug('QR code generated successfully', { url: window.location.href });
   ```

---

## 💻 CODICE PROFESSIONALE

### **1️⃣ TypeScript Strict Mode**

```json
// tsconfig.json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true
  }
}
```

### **2️⃣ ESLint + Prettier**

- ✅ **ESLint**: Configurato e attivo
- ✅ **Prettier**: Formattazione automatica
- ✅ **Pre-commit hooks**: Lint prima di commit (se abilitati)

### **3️⃣ JSDoc Completo**

**OBBLIGATORIO per tutte le funzioni pubbliche:**

```typescript
/**
 * Genera QR code per accesso mobile rapido alla brochure
 * 
 * @param canvasElement - Elemento canvas HTML dove renderizzare QR code
 * @param url - URL completo da codificare nel QR code
 * @param options - Opzioni personalizzazione QR (colori, dimensioni)
 * @returns Promise risolto al completamento della generazione
 * @throws {Error} Se canvas non è valido o URL è malformato
 * @example
 * ```typescript
 * const canvas = document.getElementById('qrCanvas') as HTMLCanvasElement;
 * await generateQRCode(canvas, 'https://brochure-icu.web.app', {
 *   width: 200,
 *   color: { dark: '#2c5f7f', light: '#ffffff' }
 * });
 * ```
 * @remarks
 * QR code nascosto su mobile/tablet con CSS @media print
 * Visibile solo in modalità stampa per facilitare accesso rapido
 */
export async function generateQRCode(
  canvasElement: HTMLCanvasElement,
  url: string,
  options: QRCodeOptions
): Promise<void> {
  // Implementation
} /*end generateQRCode*/
```

**Elementi obbligatori JSDoc:**
- `@param` - Ogni parametro con descrizione
- `@returns` - Cosa ritorna la funzione
- `@throws` - Eccezioni possibili
- `@example` - Esempio uso concreto
- `@remarks` - Note implementative (IMPORTANTE per contesto medico)

### **4️⃣ Nomi Descrittivi**

```typescript
// ✅ CORRETTO - Nomi descrittivi
const currentLanguage = locale.value;
const isItalianLocale = currentLanguage.startsWith('it');
const hospitalContactEmail = 'terapiaintensivanoa@uslnordovest.toscana.it';

// ❌ SBAGLIATO - Abbreviazioni oscure
const lang = loc.val;
const isIT = l.startsWith('it');
const email = 't@usl.it';
```

**Regole naming:**
- Variabili: `camelCase`
- Funzioni: `camelCase`
- Componenti Vue: `PascalCase`
- Costanti: `SCREAMING_SNAKE_CASE`
- Files: `PascalCase.vue` o `kebab-case.ts`

### **5️⃣ Funzioni Piccole**

```typescript
// ✅ CORRETTO - Funzione focalizzata (< 50 righe)
export function changeLanguage(lang: 'it' | 'en'): void {
  const localeMap: Record<string, string> = {
    'it': 'it-IT',
    'en': 'en-US'
  };
  
  const fullLocale = localeMap[lang];
  if (!fullLocale) {
    logger.warn('Unknown language code', { lang });
    return;
  }
  
  locale.value = fullLocale;
  logger.info('Language changed', { newLocale: fullLocale });
} /*end changeLanguage*/
```

**Regola**: Max **50 righe** per funzione. Se più lunga, spezzare in sub-funzioni.

### **6️⃣ DRY (Don't Repeat Yourself)**

```typescript
// ❌ SBAGLIATO - Codice duplicato
const italianTitle = t('brochure.title');
const englishTitle = t('brochure.title');

// ✅ CORRETTO - Funzione riutilizzabile
const getLocalizedTitle = (locale: string): string => {
  return t('brochure.title', locale);
}; /*end getLocalizedTitle*/

const italianTitle = getLocalizedTitle('it-IT');
const englishTitle = getLocalizedTitle('en-US');
```

### **7️⃣ Error Handling Completo**

```typescript
// ✅ CORRETTO - Try/catch completo
async function generateQRCodeSafe(canvasRef: Ref<HTMLCanvasElement | null>): Promise<void> {
  try {
    if (!canvasRef.value) {
      throw new Error('Canvas element not found');
    }
    
    const url = window.location.href;
    await QRCode.toCanvas(canvasRef.value, url, {
      width: 200,
      color: {
        dark: '#2c5f7f',
        light: '#ffffff'
      }
    });
    
    logger.info('QR code generated', { url });
    
  } catch (error) {
    logger.error('Failed to generate QR code', {
      error: error instanceof Error ? error.message : 'Unknown error'
    });
  }
} /*end generateQRCodeSafe*/
```

---

## 🏗️ ARCHITETTURA COMPONENTI

### **REGOLA FONDAMENTALE: Separazione Responsabilità**

#### **1️⃣ PAGES = LAYOUT E ORCHESTRAZIONE**

File `src/pages/*Page.vue` contiene **SOLO**:

```vue
<script setup lang="ts">
/**
 * @file BrochurePage.vue
 * @description Pagina principale brochure Terapia Intensiva NOA
 * Contiene layout completo con header, sezioni informative, QR code
 */

import { ref, computed, onMounted } from 'vue';
import { useI18n } from 'vue-i18n';
import QRCode from 'qrcode';
import logoSst from '../assets/logo-sst.png';
import ospedaleImg from '../assets/ospedale.png';

const { t, locale } = useI18n();
const qrCanvas = ref<HTMLCanvasElement | null>(null);

// Computed per flag corrente
const currentLang = computed(() => {
  return locale.value.startsWith('it') ? '🇮🇹 IT' : '🇬🇧 EN';
});

// Funzione cambio lingua
const changeLang = (lang: string) => {
  const localeMap: Record<string, string> = {
    'it': 'it-IT',
    'en': 'en-US'
  };
  locale.value = localeMap[lang] || lang;
};

// Generazione QR code
onMounted(async () => {
  try {
    if (qrCanvas.value) {
      await QRCode.toCanvas(qrCanvas.value, window.location.href, {
        width: 200,
        color: { dark: '#2c5f7f', light: '#ffffff' }
      });
    }
  } catch (error) {
    console.error('QR code generation failed:', error);
  }
});
</script>

<template>
  <q-page class="brochure-page">
    <!-- Header Quasar -->
    <q-header elevated class="bg-primary text-white">
      <q-toolbar>
        <q-avatar size="50px">
          <img :src="logoSst" alt="Logo SST" />
        </q-avatar>
        
        <q-toolbar-title>
          {{ t('brochure.title') }}
        </q-toolbar-title>
        
        <!-- Language Selector -->
        <q-btn-dropdown :label="currentLang" flat>
          <q-list>
            <q-item clickable v-close-popup @click="changeLang('it')">
              <q-item-section>🇮🇹 Italiano</q-item-section>
            </q-item>
            <q-item clickable v-close-popup @click="changeLang('en')">
              <q-item-section>🇬🇧 English</q-item-section>
            </q-item>
          </q-list>
        </q-btn-dropdown>
      </q-toolbar>
    </q-header>
    
    <!-- Contenuto sezioni -->
    <!-- ... resto del template ... -->
  </q-page>
</template>

<style scoped lang="scss">
.brochure-page {
  padding: 20px;
  background-color: #f5f5f5;
}
</style>
```

**✅ ALLOWED in Pages:**
- Layout: Headers, footers, navigation
- Component imports
- Event handling: Simple handlers
- State: `ref`, `computed` per UI
- Lifecycle hooks: `onMounted`, `onUnmounted`

**❌ FORBIDDEN in Pages:**
- Business logic complessa
- API calls (usare composables)
- Validazioni complesse

**Max Size**: 180-400 righe (BrochurePage.vue attualmente ~350 righe ✅)

#### **2️⃣ COMPONENTS = LOGICA + UI RIUTILIZZABILE**

Per componenti riutilizzabili (es. `ContactCard.vue`, `InfoSection.vue`):

```vue
<script setup lang="ts">
/**
 * @file ContactCard.vue
 * @description Componente card contatti ospedale riutilizzabile
 */

interface Props {
  title: string;
  phone?: string;
  email?: string;
  address?: string;
}

const props = defineProps<Props>();
</script>

<template>
  <q-card class="contact-card">
    <q-card-section>
      <div class="text-h6">{{ title }}</div>
    </q-card-section>
    
    <q-card-section v-if="phone">
      <div class="text-caption">📞 {{ phone }}</div>
    </q-card-section>
    
    <q-card-section v-if="email">
      <div class="text-caption">📧 {{ email }}</div>
    </q-card-section>
  </q-card>
</template>
```

---

## 🌍 INTERNAZIONALIZZAZIONE (i18n)

### **REGOLE OBBLIGATORIE:**

1. ✅ **Tutte le stringhe UI devono essere localizzate**
   ```vue
   <!-- ❌ SBAGLIATO -->
   <h1>Benvenuti in Terapia Intensiva</h1>
   
   <!-- ✅ CORRETTO -->
   <h1>{{ t('brochure.title') }}</h1>
   ```

2. ✅ **Namespace organizzati per feature**
   ```typescript
   // src/i18n/it-IT/brochure.ts
   export default {
     title: 'Carta d\'Accoglienza - Terapia Intensiva',
     mission: {
       title: 'La Nostra Missione',
       description: 'Fornire cure intensive di eccellenza...'
     },
     contacts: {
       title: 'Contatti',
       phone: 'Telefono',
       email: 'Email'
     }
   };
   ```

3. ✅ **Traduzioni verificate da esperti**
   - IT: Terminologia medica italiana certificata
   - EN: Medical English professionale
   - NO traduzioni automatiche non verificate

4. ✅ **Fallback per lingue mancanti**
   ```typescript
   // src/boot/i18n.ts
   export default boot(({ app }) => {
     const i18n = createI18n({
       locale: 'it-IT',
       fallbackLocale: 'it-IT', // Default italiano
       messages: {
         'it-IT': itIT,
         'en-US': enUS
       }
     });
     
     app.use(i18n);
   });
   ```

---

## 💰 ZERO COSTI - OBBLIGATORIO

**REQUISITO ASSOLUTO**: Soluzione a **COSTO ZERO**.

### **✅ SERVIZI GRATUITI APPROVATI:**

1. **Firebase Hosting** (Piano Spark - Free)
   - ✅ 10 GB storage
   - ✅ 360 MB/day transfer
   - ✅ SSL automatico
   - ✅ CDN globale
   - ✅ Custom domain gratuito

2. **Netlify** (Alternative gratuita)
   - ✅ 100 GB bandwidth/month
   - ✅ Continuous deployment
   - ✅ SSL automatico

3. **GitHub Pages** (Alternative gratuita)
   - ✅ Hosting statico illimitato
   - ✅ SSL automatico
   - ✅ Integrazione GitHub

### **❌ SERVIZI A PAGAMENTO VIETATI:**

- ❌ Firebase Blaze (pay-as-you-go)
- ❌ Vercel Pro
- ❌ AWS (senza free tier)
- ❌ Google Cloud Compute
- ❌ Heroku (piano a pagamento)

**PRINCIPIO**: Se costa anche solo 1€, **NON SI USA**.

---

## 📐 METODOLOGIA SVILUPPO

### **1️⃣ Git Workflow**

```bash
# Feature branch workflow
git checkout -b feature/add-french-translation
# ... lavoro ...
git add .
git commit -m "feat: Add French translation for brochure"
git push origin feature/add-french-translation
# Apri Pull Request su GitHub
```

**Conventional Commits:**
- `feat:` - Nuova funzionalità
- `fix:` - Bug fix
- `docs:` - Documentazione
- `style:` - Formattazione
- `refactor:` - Refactoring codice
- `test:` - Test
- `chore:` - Maintenance

### **2️⃣ Testing (OPZIONALE)**

Testing **NON è obbligatorio** per questo progetto (ambito pubblico, non critico).

Se implementato:
- Unit tests: Vitest
- Component tests: @vue/test-utils
- E2E tests: Cypress (solo se necessario)

### **3️⃣ Deployment Process**

```bash
# 1. Build produzione
yarn build

# 2. Test build locale
yarn preview

# 3. Deploy Firebase
firebase deploy

# 4. Verifica URL produzione
# https://brochure-icu.web.app
```

---

## 🎨 DESIGN SYSTEM

### **COLORI BRAND:**

```scss
// Colori primari Terapia Intensiva NOA
$primary: #2c5f7f;    // Blu principale
$secondary: #1a4460;  // Blu scuro
$accent: #e8f4f8;     // Azzurro chiaro

// Colori funzionali
$positive: #21BA45;   // Verde successo
$negative: #C10015;   // Rosso errore
$info: #31CCEC;       // Blu informazioni
$warning: #F2C037;    // Giallo warning
```

### **TIPOGRAFIA:**

```scss
// Font sizes
$text-body: 16px;     // Corpo testo (min accessibilità)
$text-caption: 12px;  // Caption
$text-h6: 1.25rem;    // Titolo sezione
$text-h5: 1.5rem;     // Titolo principale

// Font family
font-family: 'Roboto', 'Helvetica Neue', Arial, sans-serif;
```

### **SPACING:**

```scss
// Padding/Margin
$space-xs: 4px;
$space-sm: 8px;
$space-md: 16px;
$space-lg: 24px;
$space-xl: 32px;
```

---

## ✅ CHECKLIST PRE-DEPLOY

Prima di ogni deploy su produzione:

- [ ] ✅ Build senza errori (`yarn build`)
- [ ] ✅ ESLint senza warning (`yarn lint`)
- [ ] ✅ TypeScript senza errori (`yarn type-check`)
- [ ] ✅ i18n completo (IT + EN)
- [ ] ✅ Immagini ottimizzate (< 500 KB cadauna)
- [ ] ✅ Accessibilità WCAG 2.1 AA verificata
- [ ] ✅ Contrast ratio ≥ 4.5:1
- [ ] ✅ Responsive design testato (mobile/tablet/desktop)
- [ ] ✅ QR code funzionante
- [ ] ✅ Contatti ospedale aggiornati
- [ ] ✅ Link URP corretto (https://www.uslnordovest.toscana.it/)
- [ ] ✅ Nessun console.log() in produzione
- [ ] ✅ Nessuna credenziale esposta
- [ ] ✅ SSL attivo (HTTPS)
- [ ] ✅ Meta tags SEO completi
- [ ] ✅ Lighthouse score > 90 (Performance, Accessibility, SEO)

---

## 📊 PERFORMANCE TARGETS

### **Lighthouse Scores (Minimo):**

- 🟢 **Performance**: ≥ 90
- 🟢 **Accessibility**: ≥ 95 (OBBLIGATORIO ambito medico)
- 🟢 **Best Practices**: ≥ 90
- 🟢 **SEO**: ≥ 90

### **Bundle Size:**

- **Total JS**: < 300 KB (gzipped)
- **Total CSS**: < 50 KB (gzipped)
- **Images**: < 500 KB cadauna (ottimizzate WebP se possibile)

### **Load Times:**

- **First Contentful Paint (FCP)**: < 1.8s
- **Largest Contentful Paint (LCP)**: < 2.5s
- **Time to Interactive (TTI)**: < 3.8s

---

## 🔧 TROUBLESHOOTING COMUNI

### **Problema: Immagini non caricate**

```bash
# Verifica presenza file
ls -lh src/assets/*.png

# Se mancanti:
# - Aggiungi logo-sst.png
# - Aggiungi ospedale.png
```

### **Problema: QR Code non generato**

```vue
<!-- Verifica ref corretto -->
<canvas ref="qrCanvas"></canvas>

<!-- Verifica onMounted -->
<script setup>
onMounted(async () => {
  if (qrCanvas.value) {
    await QRCode.toCanvas(qrCanvas.value, window.location.href, options);
  }
});
</script>
```

### **Problema: Lingua non cambia**

```typescript
// Verifica mapping locali
const localeMap: Record<string, string> = {
  'it': 'it-IT',
  'en': 'en-US'
};

// Verifica import i18n
import { useI18n } from 'vue-i18n';
const { locale } = useI18n();
```

---

## 📚 RISORSE UFFICIALI

### **Documentazione:**

- [Vue 3](https://vuejs.org) - Framework JavaScript
- [Quasar Framework](https://quasar.dev) - UI components
- [TypeScript](https://www.typescriptlang.org/docs/) - Type system
- [vue-i18n](https://vue-i18n.intlify.dev/) - Internazionalizzazione
- [Firebase Hosting](https://firebase.google.com/docs/hosting) - Deploy

### **Accessibility:**

- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/)
- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)

### **Tools:**

- [Lighthouse](https://developers.google.com/web/tools/lighthouse) - Performance audit
- [WAVE](https://wave.webaim.org/) - Accessibility checker
- [Can I Use](https://caniuse.com/) - Browser compatibility

---

## 🚨 ALERT PRIORITÀ

### **🔴 PRIORITÀ MASSIMA (Blockers):**

- Accessibilità WCAG 2.1 AA non rispettata
- Credenziali esposte in codice
- Errori TypeScript/ESLint non risolti
- Build produzione fallita
- SSL non attivo in produzione

### **🟠 PRIORITÀ ALTA (Importante):**

- Traduzioni incomplete (IT o EN mancanti)
- Immagini non ottimizzate (> 500 KB)
- Lighthouse score < 90
- Contatti ospedale non aggiornati

### **🟡 PRIORITÀ MEDIA (Miglioramento):**

- Refactoring codice duplicato
- Documentazione JSDoc incompleta
- Performance non ottimale

### **🟢 PRIORITÀ BASSA (Nice to have):**

- Aggiunta nuove lingue (FR, DE)
- Dark mode
- PWA offline support avanzato

---

## 📝 COMMIT GUIDELINES

### **Formato Standard:**

```bash
<type>(<scope>): <subject>

<body>

<footer>
```

### **Esempi:**

```bash
# Feature
feat(i18n): Add French translation for brochure sections
- Add fr-FR/brochure.ts with complete translations
- Update i18n index.ts with fr-FR import
- Add France flag icon to language selector

# Bug Fix
fix(qrcode): Fix QR code not visible on mobile devices
- Add @media print CSS rule
- Hide QR code on screen, show only in print mode
- Update BrochurePage.vue styles

# Documentation
docs(readme): Update deployment section with Firebase guide
- Add step-by-step Firebase setup
- Include CI/CD GitHub Actions workflow
- Add troubleshooting for common deploy issues

# Refactor
refactor(components): Extract ContactCard into separate component
- Move contact card markup to src/components/ContactCard.vue
- Add TypeScript Props interface
- Improve reusability across pages
```

---

## ✨ VERSION CONTROL

### **Branching Strategy:**

```
main (produzione stabile)
  └── develop (sviluppo attivo)
       ├── feature/add-french-i18n
       ├── feature/dark-mode
       ├── fix/qr-code-mobile
       └── docs/update-readme
```

### **Merge Policy:**

1. **Feature Branch** → Pull Request → **Develop**
2. **Develop** (testato) → Pull Request → **Main**
3. **Main** → Auto-deploy Firebase (CI/CD)

---

## 🎯 OBIETTIVI PROGETTO

### **Completati ✅:**

- [x] Brochure bilingue (IT/EN)
- [x] i18n system completo
- [x] QR code generation
- [x] Responsive design
- [x] Quasar components (q-header, q-toolbar)
- [x] PNG images ottimizzate
- [x] Accessibilità WCAG 2.1 AA
- [x] Link URP aggiornato
- [x] Professional README
- [x] Firebase deploy config

### **Roadmap v1.1.0:**

- [ ] Lingua francese (FR)
- [ ] Dark mode toggle
- [ ] Esportazione PDF
- [ ] Google Analytics
- [ ] Service Worker offline
- [ ] SEO audit completo
- [ ] Performance optimization (WebP images)

---

**Made with ❤️ for better healthcare communication**

🏥 **Nuovo Ospedale Apuano - Massa (MS)**

---

**VERSION:** 1.0.0  
**LAST UPDATE:** 2025-12-09  
**MAINTAINER:** Nykyri AI  
**LICENSE:** MIT
